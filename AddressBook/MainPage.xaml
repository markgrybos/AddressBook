<Page
    x:Class="AddressBook.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AddressBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:AddressBook.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <!--<RelativePanel Grid.Row="1"
                       Grid.Column="1">
            <Button Name="HamburgerButton"
                    RelativePanel.AlignLeftWithPanel="True"
                    Content="&#xE700;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="28"
                    Height="50"
                    Width="50"/>
        <Button Name="AddButton"
                    RelativePanel.AlignRightWithPanel="True"
                    Content="&#xE710;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="28"
                    Height="50"
                    Width="50"
                    Click="AddButton_Click"
                    Background="CornflowerBlue"
                    Foreground="White"
                    Margin="0,15,15,0"/>
        </RelativePanel>-->
        <ListView x:Name="ContactList"
                              IsItemClickEnabled="True"
                              ItemsSource="{x:Bind allcontacts}"
                              SelectionMode="Single"
                              Grid.Row="1"
                              Grid.Column="0"
                              Background="CornflowerBlue"
                              BorderThickness="1"
                              Margin="5,5,0,5"
                              BorderBrush="White">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Contact">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1"
                                   Foreground="White">
                            <Run Text="{x:Bind FirstName}"></Run>
                            <Run Text="{x:Bind LastName}"></Run>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="1"
              Grid.Column="1">
            <Grid.RowDefinitions>
                
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1"
                    Grid.Row="1"
                    Background="LightCyan">

            </StackPanel>
        </Grid>
        
        <!--<SplitView Grid.Row="1"
                   Name="ContactsSplitview"
                   DisplayMode="CompactInline"
                   CompactPaneLength="350"
                   OpenPaneLength="450">
            <SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Contacts"
                               Name="ContactsHeader"
                               FontWeight="Bold"
                               FontSize="36"
                               FontFamily="Calibri"
                               Margin="150,10,0,0"/>
                    <ListView Name="ListOfLettersView"
                              IsItemClickEnabled="True"
                              ItemClick="ListOfLettersView_ItemClick"
                              ItemsSource="{x:Bind allcontacts}"
                              SelectionMode="Single"
                              Grid.Row="1">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="model:Contact">
                                <TextBlock Text="{x:Bind FirstName}"/>
                             
                             

                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </SplitView.Pane>
            
        </SplitView> -->
        <TimePicker Margin="1258,-103,0,0" VerticalAlignment="Top"/>
    </Grid>

</Page>
